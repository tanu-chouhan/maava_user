org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Google Maps key for the Android Maps SDK.
# Injected into AndroidManifest.xml as the MAPS_API_KEY manifest placeholder —
# do not paste the key into the manifest itself. Override per build with:
#   flutter build apk -Pmaps-api-key=<key>
MAPS_API_KEY=AIzaSyArBbII2fgAuVaycfkEAm1GcuyvKPTSWyc
